# SPDX-FileCopyrightText: 2026 mjutest contributors
# SPDX-License-Identifier: MIT OR Apache-2.0

[package]
name = "rust-mutants"
description = "Mutation testing engine for Rust: one instrumented snapshot, every compilable mutant behind a guard, one environment variable per test process"
keywords = ["mutation-testing", "testing", "cargo", "assurance"]
categories = ["development-tools::testing"]
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
readme.workspace = true

[features]
# Fakes and generators the suites drive the engine's own seams with. Never on for a product.
testkit = []

[dependencies]
hex = { workspace = true }
jiff = { workspace = true, features = ["serde"] }
proc-macro2 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
# `printing` is for `Spanned` alone (joined byte ranges of whole nodes);
# the engine never turns a syntax tree back into text.
syn = { workspace = true, features = ["printing"] }
thiserror = { workspace = true }

[target.'cfg(unix)'.dependencies]
rustix = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
jsonschema = { workspace = true }
rust-mutants = { workspace = true, features = ["testkit"] }
mjutest-devkit = { workspace = true }
proptest = { workspace = true }
tempfile = { workspace = true }

[[bench]]
name = "foundation"
harness = false

[[bench]]
name = "pipeline"
harness = false

[lints]
workspace = true
