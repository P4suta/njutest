# Mutation report — fixture-coverage <run>

**96.6%** — 28 detected of 29 decided

32 mutants were cataloged: 29 executed, 0 refused by the compiler, 4 places that produced no candidate.

| outcome | how many |
| --- | --- |
| killed | 28 |
| survived | 1 |
| step_limit_reached | 0 |
| waited | 0 |
| inconclusive | 0 |
| errored | 0 |
| not run | 3 |

Those 7 add to the 32 cataloged. Within them, not run is 0 unreached, not run is 3 discharged, not run is 0 declined, survived is 0 expected.

## Findings

- **discharged-mutant** — every target that could have noticed d8a9939330496216a17d was removed by a proof, so no test could have: the mutation is in code the tests run and never observe
- **surviving-mutant** — no test noticed 27e9c3f2d6bb568e5ef3; 1 test ran and passed
- **discharged-mutant** — every target that could have noticed 7cfdea86ac571b743bd0 was removed by a proof, so no test could have: the mutation is in code the tests run and never observe
- **discharged-mutant** — every target that could have noticed 0a8ab9771954a24f9d25 was removed by a proof, so no test could have: the mutation is in code the tests run and never observe

## Survivors (1)

| where | rule | change | id |
| --- | --- | --- | --- |
| `src/lib.rs:16:10` | `le-to-lt` | `<=` → `<` | `27e9c3f2d6bb568e5ef3` |

Each of these is a gap in the tests or a claim to write down: `rust-mutants explain <id>` says what it is, and `[[mutation.expect]]` accepts one with a reason.
