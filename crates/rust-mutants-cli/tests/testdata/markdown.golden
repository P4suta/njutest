# Mutation report — fixture-coverage <run>

**92.9%** — 13 detected of 14 decided

| what | how many |
| --- | --- |
| cataloged | 16 |
| killed | 13 |
| survived | 1 |
| timed out | 0 |
| inconclusive | 0 |
| errored | 0 |
| not run | 2 |
| unreached | 0 |
| discharged | 2 |
| expected | 0 |
| refused | 0 |
| skipped | 3 |

## Findings

- **discharged-mutant** — every target that could have noticed 607f27ebae423d86a907 was removed by a proof, so no test could have: the mutation is in code the tests run and never observe
- **surviving-mutant** — no test noticed 095fbfefaa7749ebb142; 1 test ran and passed
- **discharged-mutant** — every target that could have noticed 5c32d72280e91a9530b0 was removed by a proof, so no test could have: the mutation is in code the tests run and never observe

## Survivors (1)

| where | rule | change | id |
| --- | --- | --- | --- |
| `src/lib.rs:16:10` | `le-to-lt` | `<=` → `<` | `095fbfefaa7749ebb142` |

Each of these is a gap in the tests or a claim to write down: `rust-mutants explain <id>` says what it is, and `[[mutation.expect]]` accepts one with a reason.
