Mutation testing for Rust and Cargo.

rust-mutants copies the workspace into a disposable snapshot, instruments every compilable mutant of
the selected files once, builds the test binaries once, and activates one mutant per test process
through an environment variable. The source workspace is never modified.

Usage: rust-mutants [OPTIONS] <COMMAND>

Commands:
  list         List the candidates that will be cataloged, before the compiler has ruled. What a
               configuration or an annotation removed is `why-skipped`
  equivalence  Ask the compiler, for every mutant, whether it renders it identically to the code it
               mutates
  catalog      Catalog the mutants the compiler accepts, and the candidates it refused with its own
               words
  run          Run the mutants and report what the tests noticed. Every accepted mutant unless one
               is named
  explain      Say everything known about one mutant
  instrument   Print one file as the engine rewrites it, guards and runtime included
  why-skipped  Tally why places were passed over
  init         Write a `.rust-mutants.toml` whose every value is already the default
  replay       Put one finding back to the tests, from what a stored run said about it
  doctor       Say what a run would find in this environment: the toolchain, the configuration, the
               temporary directory
  merge        Combine the reports of the parts of one catalog into the report the whole would have
               written
  trace        Read a recording back: what it counted, what each phase took, and what moved between
               two of them
  report       Read back a stored run report
  rules        List the operators this release knows, with the tier and version that pin them
  diagnostics  Gather everything one run established into one directory, for a bug report
  cache        Say what the engine left in the temporary directory, and remove what no run still
               owns
  help         Print this message or the help of the given subcommand(s)

Options:
      --color <WHEN>
          Whether to paint the output. `auto` paints a terminal that has not set `NO_COLOR`

          Possible values:
          - auto:   Colour a terminal that has not asked to go without
          - always: Always
          - never:  Never
          
          [default: auto]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Exit codes:
  0    every mutant the run decided, the tests noticed
  1    there is a finding about the tests: a survivor, a mutation no test reached or a proof
  removed, a mutation the run could not decide either way, or a stale or unmatched claim
  2    the run could not measure a mutation it ran — it waited, reached its step limit, errored or
  was not run — or the run itself failed, or the command was used wrongly
  130  it was interrupted
  143  it was terminated, which is what a cancelled job sends
