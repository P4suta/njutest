Every sentence a watched seam puts in front of a person.
Written by crates/njutest/tests/wire_gallery.rs; UPDATE_GOLDEN=1 rewrites it.

=== a suite that holds nothing up — the specification

What 20260918t090000z-aaaaaa did, as the seams this run watched saw it.

  exchange 0 of cache — nobody holds this up; 4 question(s) nothing noticed
  POST /orders on payments answers 201 — nobody holds this up; 6 question(s) nothing noticed
  GET /orders/1 on payments answers 200 — nobody holds this up; 7 question(s) nothing noticed

3 observed, 3 that nothing would notice changing.

=== a suite that holds nothing up — what each question came to

  payments seq 0 truncate-response POST /orders -> unnoticed
  payments seq 0 delay-response POST /orders -> unnoticed
  payments seq 0 drop-connection POST /orders -> unnoticed
  payments seq 0 replay-request POST /orders -> unnoticed
  payments seq 0 status-server-error POST /orders -> unnoticed
  payments seq 0 status-not-found POST /orders -> unnoticed
  payments seq 1 truncate-response GET /orders/1 -> unnoticed
  payments seq 1 delay-response GET /orders/1 -> unnoticed
  payments seq 1 drop-connection GET /orders/1 -> unnoticed
  payments seq 1 replay-request GET /orders/1 -> unnoticed
  payments seq 1 stale-response GET /orders/1 -> unnoticed
  payments seq 1 status-server-error GET /orders/1 -> unnoticed
  payments seq 1 status-not-found GET /orders/1 -> unnoticed
  cache seq 0 truncate-response (unread protocol) -> unnoticed
  cache seq 0 delay-response (unread protocol) -> unnoticed
  cache seq 0 drop-connection (unread protocol) -> unnoticed
  cache seq 0 replay-request (unread protocol) -> unnoticed

=== a suite that holds every seam up — the specification

What 20260918t090000z-aaaaaa did, as the seams this run watched saw it.

  exchange 0 of cache — held up by payments/test/orders
  POST /orders on payments answers 201 — held up by payments/test/orders
  GET /orders/1 on payments answers 200 — held up by payments/test/orders

3 observed, 0 that nothing would notice changing.

=== a suite that holds every seam up — what each question came to

  payments seq 0 truncate-response POST /orders -> proved (no-body-to-cut)
  payments seq 0 delay-response POST /orders -> tests (payments/test/orders)
  payments seq 0 drop-connection POST /orders -> tests (payments/test/orders)
  payments seq 0 replay-request POST /orders -> tests (payments/test/orders)
  payments seq 0 status-server-error POST /orders -> tests (payments/test/orders)
  payments seq 0 status-not-found POST /orders -> tests (payments/test/orders)
  payments seq 1 truncate-response GET /orders/1 -> proved (no-body-to-cut)
  payments seq 1 delay-response GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 drop-connection GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 replay-request GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 stale-response GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 status-server-error GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 status-not-found GET /orders/1 -> tests (payments/test/orders)
  cache seq 0 truncate-response (unread protocol) -> proved (no-body-to-cut)
  cache seq 0 delay-response (unread protocol) -> tests (payments/test/orders)
  cache seq 0 drop-connection (unread protocol) -> tests (payments/test/orders)
  cache seq 0 replay-request (unread protocol) -> tests (payments/test/orders)

=== a run that could not put its questions — the specification

What 20260918t090000z-aaaaaa did, as the seams this run watched saw it.

  exchange 0 of cache — nobody holds this up; 4 question(s) the run could not put
  POST /orders on payments answers 201 — nobody holds this up; 6 question(s) the run could not put
  GET /orders/1 on payments answers 200 — nobody holds this up; 7 question(s) the run could not put

3 observed, 0 that nothing would notice changing, 3 this run established nothing about.

=== a run that could not put its questions — what each question came to

  payments seq 0 truncate-response POST /orders -> unreached
  payments seq 0 delay-response POST /orders -> unreached
  payments seq 0 drop-connection POST /orders -> unreached
  payments seq 0 replay-request POST /orders -> unreached
  payments seq 0 status-server-error POST /orders -> unreached
  payments seq 0 status-not-found POST /orders -> unreached
  payments seq 1 truncate-response GET /orders/1 -> unreached
  payments seq 1 delay-response GET /orders/1 -> unreached
  payments seq 1 drop-connection GET /orders/1 -> unreached
  payments seq 1 replay-request GET /orders/1 -> unreached
  payments seq 1 stale-response GET /orders/1 -> unreached
  payments seq 1 status-server-error GET /orders/1 -> unreached
  payments seq 1 status-not-found GET /orders/1 -> unreached
  cache seq 0 truncate-response (unread protocol) -> unreached
  cache seq 0 delay-response (unread protocol) -> unreached
  cache seq 0 drop-connection (unread protocol) -> unreached
  cache seq 0 replay-request (unread protocol) -> unreached

=== one seam held up and one not — the specification

What 20260918t090000z-aaaaaa did, as the seams this run watched saw it.

  exchange 0 of cache — nobody holds this up; 4 question(s) nothing noticed
  POST /orders on payments answers 201 — held up by payments/test/orders
  GET /orders/1 on payments answers 200 — held up by payments/test/orders

3 observed, 1 that nothing would notice changing.

=== one seam held up and one not — what each question came to

  payments seq 0 truncate-response POST /orders -> tests (payments/test/orders)
  payments seq 0 delay-response POST /orders -> tests (payments/test/orders)
  payments seq 0 drop-connection POST /orders -> tests (payments/test/orders)
  payments seq 0 replay-request POST /orders -> tests (payments/test/orders)
  payments seq 0 status-server-error POST /orders -> tests (payments/test/orders)
  payments seq 0 status-not-found POST /orders -> tests (payments/test/orders)
  payments seq 1 truncate-response GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 delay-response GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 drop-connection GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 replay-request GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 stale-response GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 status-server-error GET /orders/1 -> tests (payments/test/orders)
  payments seq 1 status-not-found GET /orders/1 -> tests (payments/test/orders)
  cache seq 0 truncate-response (unread protocol) -> unnoticed
  cache seq 0 delay-response (unread protocol) -> unnoticed
  cache seq 0 drop-connection (unread protocol) -> unnoticed
  cache seq 0 replay-request (unread protocol) -> unnoticed
