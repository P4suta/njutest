# https://editorconfig.org — shared editor defaults, mirroring rustfmt.toml.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.rs]
max_line_length = 100

# Markdown hard line-breaks and golden files must keep their bytes.
[*.{md,txt,golden,input}]
trim_trailing_whitespace = false

[*.{yml,yaml,json}]
indent_size = 2
